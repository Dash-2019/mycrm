import { kihouList } from "./kihouList";
import { kihouShow } from "./kihouShow";

export default {
    list: kihouList,
    show: kihouShow,
    recordRepresentation: "id",
    // recordRepresentation: (record: Customer) =>
    //   `${record.first_name} ${record.last_name}`,
  
  };
  