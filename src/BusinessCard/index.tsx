import { CardList } from "./CardList";
import { CardShow } from "./CardShow";

export default {
    list: CardList,
    show: CardShow,
    // recordRepresentation: "supplier_id",
    // recordRepresentation: (record: Customer) =>
    //   `${record.first_name} ${record.last_name}`,
  
  };
  